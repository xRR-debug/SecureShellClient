# SecureShellClient

## Обзор проекта
Интерактивный клиент-сервер на Go для удаленного управления Shell. Весь трафик между клиентом и сервером проходит через защищенный **TLS-туннель**, что предотвращает перехват логинов, паролей и команд.

## ## Основные возможности

*   **🔒 TLS + Прокси:** Полное шифрование данных между Windows и Linux. Локальный мост на `127.0.0.1:2323`.
*   **🔑 Базовая авторизация:** Встроенная проверка `Login/Password` перед доступом к системе.
*   **📡 Авто-инъекция Telnet IAC:** Программное подавление локального эха (Double Echo Fix) после входа.
*   **🛡 Smart Input Filter:** Исправляет "болезни" Windows Telnet: фильтрует `\r\0` и нормализует `\n`.
*   **🚪 Автозавершение:** Основное окно закрывается автоматически через секунду после выхода из терминала.

---

## 💻 Совместимость (Platform Support)


| Компонент | ОС | Роль |
|:---:|:---:|:---|
| **Сервер (Server)** | 🐧 **Linux** | Управляет **PTY** (псевдо-терминалами) и запускает `bash`. |
| **Клиент (Client)** | 🪟 **Windows** | Работает через стандартный `telnet.exe` с поддержкой UTF-8. |

---

## 🔐 Настройка безопасности (TLS)

Для работы шифрования необходимо создать сертификаты на стороне сервера:

```bash
openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes
Use code with caution.

Разместите key.pem и cert.pem в папке с сервером.
🛠 Инструкции по компиляции
Сборка Сервера (Linux)
bash
go build -o server_linux main_server.go
chmod +x server_linux
./server_linux
Use code with caution.

Сборка Клиента (Windows)
Стандартная (с логами событий):
powershell
go build -o SecureShell.exe main.go
Use code with caution.

Скрытая (без черного окна логов):
powershell
go build -ldflags="-H windowsgui" -o SecureShell_Silent.exe main.go
Use code with caution.

⚡️ Быстрый старт
Запустите Server на удаленной машине Linux.
Запустите SecureShell.exe на Windows.
Дождитесь открытия окна Telnet.
Введите логин/пароль (по умолчанию admin / 12345).
Работайте в Bash. При закрытии окна терминала программа завершится сама.
🔧 Включение Telnet в Windows
Если telnet не найден, выполните в PowerShell от Администратора:
powershell
Enable-WindowsOptionalFeature -Online -FeatureName TelnetClient
Use code with caution.


Нужно ли подготовить **.bat файл** для автоматической компиляции клиента под обе версии (обычную и скрытую) одной командой?





## Основные возможности

*   **🔒 TLS + Локальный прокси:** Полное шифрование данных между Windows и Linux. Локальный прокси на `127.0.0.1:1337`.
*   **🔑 Базовая авторизация:** Встроенная проверка логина и пароля на стороне сервера перед предоставлением доступа к Shell.

## Требования к системе
1.  **Go:** версия 1.18 или выше.
2.  **Windows:** Включенный компонент "Клиент Telnet".
3.  **Linux:** Установленные пакеты `xterm` и `telnet`.

## 💻 Совместимость 

| Компонент | ОС | Описание |
|:---:|:---:|:---|
| **Сервер** | 🐧 **Linux** | Требуется поддержка **PTY** (псевдо-терминалов) и оболочка `bash`. |
| **Клиент** | 🪟 **Windows** | Оптимизирован для работы со стандартным `telnet.exe` и кодировкой UTF-8. |


## Установка и сборка

## Клонирование репозитория
```bash
git clone https://github.com/xRR-debug/SecureShellClient
cd SecureShellClient
```

## 🛠 Инструкции по компиляции

Вы можете скомпилировать проект под любую операционную систему, используя стандартные инструменты Go.

## 🔐 Настройка безопасности (TLS)

Для работы сервера необходимо сгенерировать SSL-сертификаты. Программа использует самоподписанный сертификат для шифрования трафика между клиентом и сервером.

### Генерация сертификатов
Выполните следующую команду в терминале (требуется установленный `openssl`):

```bash
openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes
```

Полученные файлы key.pem и cert.pem, положить рядом с client и server.

### Сборка Сервера на Linux
```bash
go build -o server server.go
chmod +x server
./server
```

### Сборка Клиента на Windows
```bash
# Стандартная сборка с консолью логов
go build -o client.exe client.go

# Скрытая сборка (только окно терминала)
go build -ldflags="-H windowsgui" -o client_silent.exe client.go
```
